# Start

pnpm run start dev

nest start

nest start:dev

# Generate
nest < generate | g > < module | mo >  < module name >

nest < generate | g > < controller | co > < controller name> 
nest < generate | g > < controller | co > < controller name>   

nest < generate | g > < service | s > < service name>
nest < generate | g > < service | s > < service name> --no--spec